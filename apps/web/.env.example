# ─── API ──────────────────────────────────────────────────────────────────────
# Base URL of the backend API server (no trailing slash)
# The admin SPA uses this to make authenticated API requests.
VITE_API_URL=http://localhost:3000/api/admin
